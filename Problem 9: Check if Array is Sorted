arr = [10, 20, 25, 40, 50]

is_sorted = True

for i in range(len(arr)-1):
    if arr[i] > arr[i + 1]:
        is_sorted = False
        break 
        
if is_sorted:
    print("array was sorted")
    
else:
    print("array was not sorted")
